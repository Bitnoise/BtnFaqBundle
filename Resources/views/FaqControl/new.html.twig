{% from 'BtnCrudBundle::breadcrumb.html.twig' import breadcrumb %}
{% extends '::control.html.twig' %}

{% form_theme form 'BtnCrudBundle:Form:fields.html.twig' %}

{% block content %}

    <form action="{{ path('cp_faq_create') }}" method="post" {{ form_enctype(form) }} class="well form-horizontal" novalidate>
          <fieldset>
              <legend>{% trans %}faq.create{% endtrans %}</legend>
              {{ form_widget(form) }}
              <p>
                  <button type="submit" class="btn btn-success">{% trans %}crud.create{% endtrans %}</button>
              </p>
        </fieldset>
    </form>

            <a href="{{ path('cp_faq') }}" class="btn">
        &laquo; {% trans %}crud.back{% endtrans %}
    </a>

{% endblock %}

{% block breadcrumb %}
    {{ breadcrumb({'cp_faq': 'crud.breadcrumb.list', 'cp_faq_new': 'crud.breadcrumb.new'}) }}
{% endblock %}
